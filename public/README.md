Static assets served from the site root.

The app icon lives at `app/icon.svg`; Next.js turns that into the favicon and
the tab icon automatically. `favicon.svg` here is the same mark, for anything
that requests it by the classic path.
